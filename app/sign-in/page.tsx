//this can be wrapped in an Authenticator component in case you don't want to use the default authenticator on the provider
//import { Authenticator } from "@aws-amplify/ui-react";
export default function SignIn() {
  return <>Welcome</>;
}
